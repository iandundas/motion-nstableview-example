# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/osx'

require 'bundler'
Bundler.require # bit.ly/M4P7Xk

Motion::Project::App.setup do |app|
  app.name = 'RubyMotion OSX TableView Example'
end
